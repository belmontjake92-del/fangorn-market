{
  "name": "@fangorn-market/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Fangorn Market onchain settlement — PriceOracle + SettlementLedger (Arbitrum Sepolia).",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network arbitrumSepolia",
    "deploy:robinhood": "hardhat run scripts/deploy.ts --network robinhoodTestnet",
    "typecheck": "hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "@types/chai": "^4.3.20",
    "@types/mocha": "^10.0.10",
    "@types/node": "^24.10.1",
    "chai": "^4.5.0",
    "dotenv": "^17.2.3",
    "hardhat": "^2.22.17",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
