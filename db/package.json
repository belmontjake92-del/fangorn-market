{
  "name": "@fangorn-market/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "SQLite (node:sqlite) data layer - indexer mirror + marketplace tables, typed repository.",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test --import tsx ./src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fangorn-market/shared": "workspace:*",
    "viem": "^2.47.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.21.0",
    "typescript": "^5.8.3"
  }
}
